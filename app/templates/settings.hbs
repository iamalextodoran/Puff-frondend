<div class="layout-row layout-xs-column">
  <div class="flex container">
    <h1>Settings</h1>
    <p>with great power comes great responsibility</p>
  </div>
</div>

<div class="layout-row layout-xs-column">
  <div class="flex container">

    <div class="card">
      <h3>Theme</h3>

      {{#paper-item as |controls|}}
        {{paper-icon "brightness-2"}}
        <p>Darkmode</p>
        <div class="md-secondary-container">
          {{#paper-switch value=darkModeToggle onChange=(action (mut darkModeToggle))}}{{/paper-switch}}
        </div>
      {{/paper-item}}

      {{#paper-item as |controls|}}
        {{paper-icon "landscape"}}
        <p>Somth</p>
        <div class="md-secondary-container">
          {{#paper-switch value=darkModeToggle onChange=(action (mut darkModeToggle))}}{{/paper-switch}}
        </div>
      {{/paper-item}}

      {{#paper-item as |controls|}}
        {{paper-icon "call-split"}}
        <p>Red flag value</p>
        <div class="flex layout layout-align-center-center slider-container">
          <p>V</p>
          {{paper-slider class="flex" min=0 max=3000 value=dangerValue onChange=(action (mut dangerValue))}}
          {{paper-input type="number" value=dangerValue onChange=(action (mut dangerValue))}}   
        </div>
      {{/paper-item}}

      {{#paper-item as |controls|}}
        {{paper-icon "landscape"}}
        <p>Accent color <span style={{colorStyle}}>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;</span></p>

        <div class="flex">
          <div class="layout layout-align-center-center slider-container">
            <p>R</p>
            {{paper-slider class="flex" min=0 max=255 value=color.red onChange=(action (mut color.red))}}
            {{paper-input type="number" value=color.red onChange=(action (mut color.red))}}
          </div>

          <div class="layout layout-align-center-center slider-container">
            <p>G</p>
            {{paper-slider class="flex md-accent" min=0 max=255 value=color.green onChange=(action (mut color.green))}}
            {{paper-input type="number" value=color.green  onChange=(action (mut color.green))}}
          </div>

          <div class="layout layout-align-center-center slider-container">
            <p>B</p>
            {{paper-slider class="flex md-primary" min=0 max=255 value=color.blue onChange=(action (mut color.blue))}}
            {{paper-input type="number" value=color.blue onChange=(action (mut color.blue))}}
          </div>
        </div>
      {{/paper-item}}

    </div>
  </div>
</div>

<div class="layout-row layout-xs-column">
  <div class="flex container">

    <div class="card">
      <h3>Users</h3>

      {{!-- {{#each users as |user|}}
        {{#paper-item class="md-3-line" onClick=(action "goToUser" item)}}
          <img src={{user.picture}} alt={{user.fullName}} class="md-avatar">
          <div class="md-list-item-text">
            <h3>{{user.fullName}}</h3>
            <h4>{{user.email}}</h4>
            <p>Some notes here.</p>
          </div>
        {{/paper-item}}
      {{/each}} --}}
      {{currentUser}}

      <div class="flex">
        {{#paper-select
          label="Selected user"
          options=userNames
          onChange=(action (mut selectedUser))
          selected=selectedUser
          as |state|}}
          {{state}}
        {{/paper-select}}
      </div>

    </div>

  </div>
</div>